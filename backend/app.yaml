runtime: python39  # 使用 Python 3.9 运行环境

entrypoint: gunicorn -b :$PORT myproject.wsgi:application  # 应用入口点

handlers:  # URL 处理规则
- url: /static
  static_dir: static/
- url: /.*
  script: auto

env_variables:  # 环境变量
  DJANGO_SETTINGS_MODULE: 'myproject.settings'

